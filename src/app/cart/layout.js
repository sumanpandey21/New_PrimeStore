

export const metadata = {
  title: "PrimeStore Shopping Cart",
  description: "Cart",
};

export default function Layout({ children }) {
  return (
    <>
      <main>{children}</main>
    </>
  )
}
